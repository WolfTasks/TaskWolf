# Bounded, explicit heap for the Kotlin compiler daemon. Applies to every
# backend build (local dev, backend-test, bootJar); it became necessary
# because the daemon under-sized and OOM'd under CodeQL's build tracer.
kotlin.daemon.jvmargs=-Xmx4g
